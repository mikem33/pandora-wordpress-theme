/* ---------------------------- *\
    
    $Editor

    Only loaded inside the block editor. Nothing here reaches the front end.

\* ---------------------------- */

// The URL field overflows its panel once it holds a full address
.block-editor-url-input
    width 100%

    input[type="text"],
    input[type="url"]
        width 100%

.block-editor-url-input__suggestions
    max-width 100%
