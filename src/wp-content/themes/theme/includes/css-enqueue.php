<?php
    /**
     * Stylesheets for a single template.
     *
     * Every .styl in assets/css/styl/pages/ compiles to assets/css/pages/, and
     * is loaded from here only where it is needed, so a page does not carry the
     * CSS of the rest of the site. Uncomment and adjust:
     */

    // if ( is_page_template( 'page-templates/template-custom-blocks.php' ) ) {
    //     wp_enqueue_style(
    //         '{{theme_prefix}}-custom-blocks-style',
    //         get_stylesheet_directory_uri() . '/assets/css/pages/custom-blocks.css',
    //         array(),
    //         $release,
    //         'all'
    //     );
    // }
